language: ruby
rvm:
 - 2.2.2
 
env:
 - CI_REPORTS=shippable/testresults COVERAGE_REPORTS=shippable/codecoverage

build:
  pre_ci:
    - docker images
  ci:
    - bundle install
    - bundle exec rake
